1. Primitive Data Types

What are the primitive data types in TypeScript?

What is the difference between null and undefined in TypeScript?

Can you assign a number to a variable of type string?

How is bigint different from number? When would you use it?

What is symbol? Why is it used in TypeScript?

Can you assign a void function to a variable? Explain with example.

What is the difference between any and unknown types?

2. Object Data Types

What is a tuple in TypeScript? Give an example.

What is the difference between an array and a tuple in TypeScript?

How do you define an object type in TypeScript? Give an example.

Can object properties be optional? How do you define optional properties?

What is the difference between interface and type alias for objects?

3. Special Data Types

What is the void type used for in TypeScript?

What is the never type? Give a scenario where it is used.

How is any different from unknown? Which is safer to use?

Can you assign undefined or null to a variable of type void?

4. Advanced Types

What is an enum in TypeScript? Give an example.

Can enum values be strings or numbers only?

What is a literal type? Give an example of a string literal type.

What is a union type? Give an example combining string and number.

What is an intersection type? Give an example combining two object types.

How do you use type aliases to simplify complex types?

Can you mix literal, union, and enum types in a single variable? Explain.

5. Function Related Types

How do you define the type of a function in TypeScript?

What is the difference between a function returning void and never?

How do you type a function that takes multiple arguments of different types?

6. Classes & OOP Concepts

What are access modifiers in TypeScript and how do they work?

Explain inheritance in TypeScript with an example.

What are static properties/methods in classes?

How do getters and setters work in TypeScript?

How do interfaces work with classes?

How does TypeScript enforce type safety in class hierarchies?

7. Promises & Asynchronous Programming

What is a Promise in TypeScript?

What are the three states of a Promise?

How do you handle fulfilled and rejected Promises?

How does async/await work with Promises?

How do you type a Promise that resolves with a number, string, or object?

Explain Promise chaining and why it is useful.

What is the difference between Promise.all, Promise.race, and Promise.allSettled?

How do you handle errors in a chain of promises?

How can generics enhance promise return types?

How do you type external API responses safely with promises?

8. Generics & Type Utilities

How do conditional types work?

What is the purpose of infer?

How do keyof T and T[K] differ?

How are utility types implemented internally?

When should generics be avoided?

Explain deep readonly and its use cases.

How do mapped types work?

How can generics be used with classes (e.g., repository pattern)?

9. Miscellaneous & Conceptual

Can tuple elements be optional or readonly? Give an example.

What happens if you try to assign a value of one type to a variable of another type?

How does TypeScript enforce type safety at compile time?

Why would you choose unknown over any in TypeScript?

How do you propagate types through promise chains?

How do you handle errors in async/await using TypeScript?

What are best practices for large-scale asynchronous TypeScript applications?